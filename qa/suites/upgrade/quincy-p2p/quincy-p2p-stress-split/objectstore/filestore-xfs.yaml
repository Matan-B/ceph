overrides:
  ceph:
    fs: xfs
    conf:
      osd:
        osd objectstore: filestore
        osd sloppy crc: true
        debug filestore: 20
        debug rocksdb: 10
  ceph-deploy:
    fs: xfs
    filestore: True
    conf:
      osd:
        osd objectstore: filestore
        osd sloppy crc: true

